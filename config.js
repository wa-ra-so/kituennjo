// Fill in your own free Yahoo! JAPAN Client ID to enable the "この範囲を
// Yahoo!ロコから検索" button, which finds indoor smoking rooms (department
// stores, shopping malls, etc.) that OpenStreetMap usually doesn't have.
//
// Get a Client ID (free, no billing) at:
//   https://e.developer.yahoo.co.jp/register
// Free tier: 50,000 requests/day per application (see README for details).
//
// IMPORTANT: this file is served as-is to every visitor's browser — this is
// a static site with no backend, so whatever you put here is publicly
// visible in the page source (view-source / devtools). A Yahoo! Client ID
// has no billing attached, so the worst case if someone copies it is your
// own free daily quota being used up, not a bill. Still, treat it as public.
//
// Leave the string empty to keep the button hidden (the default).
window.APP_CONFIG = {
  yahooClientId: "",
};
